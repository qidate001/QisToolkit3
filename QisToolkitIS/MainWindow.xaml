<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="QisToolkitIS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="CMD Console">

    <Grid Padding="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 输入 -->
        <Grid ColumnSpacing="10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="CommandBox"
                PlaceholderText="请输入 CMD 命令..."
                KeyDown="CommandBox_KeyDown"/>

            <Button
                Grid.Column="1"
                Content="执行"
                Click="Execute_Click"/>

        </Grid>

        <!-- 控制台 -->

        <RichEditBox
            x:Name="ConsoleBox"
            Grid.Row="1"
            Margin="0,10,0,0"
            IsReadOnly="True"
            IsSpellCheckEnabled="False"
            FontFamily="Consolas"
            FontSize="14"
            Background="Black"
            Foreground="Lime"
            BorderThickness="0"
            VerticalAlignment="Stretch"/>

    </Grid>

</Window>